# ─────────────────────────────────────────────
# SIX CONTINENTS CHALLENGE – ENV TEMPLATE
# Last verified stable: v1.3-stable-2025-11-10
# ─────────────────────────────────────────────

# ---------- SUPABASE CONNECTION ----------
# Public URL of your Supabase project (no secret info)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# Public anon key – safe to expose in client build
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Service role key – ⚠️ SERVER-SIDE ONLY ⚠️
# Never expose in client-side JS or push to GitHub.
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# ---------- VALIDATION / SECURITY ----------
# Salt used in the answer hash process
# (Do NOT change unless you re-hash all answers)
ANSWER_SALT=REPLACE_WITH_REAL_SALT

# ---------- OPTIONAL SETTINGS ----------
# e.g., Logging, debug mode, feature flags
DEBUG_MODE=false
